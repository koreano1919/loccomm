version: '2'
services:
  loccommcart-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/loccommCart/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=loccommCart
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
