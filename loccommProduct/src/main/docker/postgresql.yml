version: '2'
services:
  loccommproduct-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/loccommProduct/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=loccommProduct
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
